# Le contenu vit dans l'image, jamais en bind mount : une image a un digest,
# s'atteste et se déploie par digest ; un répertoire déposé sur l'hôte, non.
FROM ghcr.io/static-web-server/static-web-server:2
COPY public/ /public/
